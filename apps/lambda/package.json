{
  "name": "@planty/lambda",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "format": "prettier --debug-check . --ignore-path ../../.gitignore",
    "lint": "eslint",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "build": "esbuild index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.588.0",
    "@planty/eslint-config": "workspace:*",
    "@planty/prettier-config": "workspace:*",
    "@planty/tsconfig": "workspace:*",
    "@types/aws-lambda": "^8.10.138",
    "expo-server-sdk": "^3.10.0",
    "prettier": "^3.2.5",
    "tailwindcss": "^3.4.3",
    "typescript": "^5.3.3"
  },
  "prettier": "@planty/prettier-config",
  "dependencies": {
    "@planty/db": "workspace:*",
    "aws-lambda": "^1.0.7",
    "moment": "^2.30.1"
  }
}
